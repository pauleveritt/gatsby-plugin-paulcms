type BlogPost implements Node & Resource {
    id: ID!
    slug: String!
    title: String!
    body: String! @parentbody
    parent: Node
}

type Author implements Node & Resource {
    id: ID!
    slug: String!
    title: String!
    body: String! @parentbody
    parent: Node
}
